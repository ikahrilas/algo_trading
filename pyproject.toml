[project]
name = "algo-trading"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "logging>=0.4.9.6",
    "nest-asyncio>=1.6.0",
    "omega",
    "riskfolio-lib>=7.2.0",
    "zipline-reloaded>=3.1.1",
]

[tool.uv.sources]
omega = { git = "ssh://git@github.com/quant-science/omega.git" }
